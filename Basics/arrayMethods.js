//map method
const numbers = [1, 2, 3, 4, 5];
const doubledNumbers = numbers.map((num) => {
  return num * 2;
});
//filter method

const evenNumbers = numbers.filter((num) => {
  return num % 2 === 2;
});
